# Fixed Bins (Strategy F)
- Assigning specific material to a specific bin via [MM02] or [WM01].

# Addition to Existing Stock (Strategy I)
- The system always looks for a bin that already contains the same material, if there is space, it adds it there.

# Empty Storage Bin (Strategy L)
- The system finds the next available empty bin in the warehouse.
